package com.company;

/**
 * How to further polymorphism without relying the inheritance class hierarchy
 * can add a class that has nothing in common with base class but have the same behavior
 */
public interface Talking
{
    void talk();
}
